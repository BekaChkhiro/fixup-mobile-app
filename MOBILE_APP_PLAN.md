# Fix My Ride Georgia - React Native აპლიკაციის გეგმა

## მიმოხილვა

აპლიკაცია არის Fix My Ride Georgia ვებსაიტის მობილური ვერსია, რომელიც ფოკუსირებულია სამ ძირითად ფუნქციაზე: სერვისების დათვალიერება, ინტერაქტიული რუკა და საწვავის ფასები.

---

## ტექნიკური სპეციფიკაციები

### ტექნოლოგიური სტეკი

| კომპონენტი | ტექნოლოგია |
|-----------|------------|
| Framework | React Native + Expo |
| პლატფორმები | iOS და Android |
| Backend | Supabase (არსებული) |
| State Management | React Query (@tanstack/react-query) |
| Navigation | React Navigation v6 |
| რუკა | react-native-maps + OpenStreetMap tiles |
| UI ბიბლიოთეკა | React Native Paper / NativeWind |
| ენა | TypeScript |
| ფორმები | React Hook Form + Zod |

### Expo SDK
- expo-location (მდებარეობის სერვისები)
- expo-linking (ტელეფონზე დარეკვა, Google Maps გახსნა)
- expo-image (ოპტიმიზებული სურათები)
- expo-constants
- expo-status-bar

---

## აპლიკაციის სტრუქტურა

```
fix-my-ride-mobile/
├── app/                          # Expo Router (file-based routing)
│   ├── (tabs)/                   # Tab Navigator
│   │   ├── _layout.tsx           # Tab navigation config
│   │   ├── index.tsx             # სერვისები (Home)
│   │   ├── map.tsx               # რუკა
│   │   └── fuel.tsx              # საწვავი
│   ├── service/
│   │   └── [id].tsx              # სერვისის დეტალები
│   ├── category/
│   │   └── [slug].tsx            # კატეგორიის სერვისები
│   ├── mechanic/
│   │   └── [id].tsx              # მექანიკის პროფილი
│   ├── laundry/
│   │   └── [id].tsx              # სამრეცხაოს დეტალები
│   ├── drive/
│   │   └── [id].tsx              # დრაივის დეტალები
│   └── _layout.tsx               # Root layout
├── components/
│   ├── ui/                       # ბაზისური კომპონენტები
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   ├── Badge.tsx
│   │   ├── Skeleton.tsx
│   │   └── ...
│   ├── services/
│   │   ├── ServiceCard.tsx
│   │   ├── ServiceList.tsx
│   │   ├── ServiceFilters.tsx
│   │   ├── CategoryGrid.tsx
│   │   └── ServiceGallery.tsx
│   ├── map/
│   │   ├── MapView.tsx
│   │   ├── CustomMarker.tsx
│   │   ├── MapFilters.tsx
│   │   ├── MapSidebar.tsx
│   │   └── MarkerCallout.tsx
│   ├── fuel/
│   │   ├── FuelPriceCard.tsx
│   │   ├── FuelImporterList.tsx
│   │   └── FuelPriceComparison.tsx
│   └── common/
│       ├── Header.tsx
│       ├── SearchBar.tsx
│       ├── EmptyState.tsx
│       ├── ErrorState.tsx
│       └── LoadingState.tsx
├── hooks/
│   ├── useServices.ts
│   ├── useMechanics.ts
│   ├── useLaundries.ts
│   ├── useDrives.ts
│   ├── useFuelImporters.ts
│   ├── useCategories.ts
│   ├── useCities.ts
│   └── useLocation.ts
├── services/
│   └── supabase.ts               # Supabase client
├── types/
│   └── index.ts                  # TypeScript types (from web app)
├── utils/
│   ├── formatters.ts
│   ├── mapHelpers.ts
│   └── phoneHelpers.ts
├── constants/
│   ├── colors.ts
│   ├── mapConfig.ts
│   └── categories.ts
└── assets/
    ├── images/
    └── fonts/
```

---

## ნავიგაციის სტრუქტურა

### Bottom Tab Navigator (3 ტაბი)

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│              [ძირითადი კონტენტი]                    │
│                                                     │
├─────────────────────────────────────────────────────┤
│  🔧 სერვისები  │  🗺️ რუკა  │  ⛽ საწვავი           │
└─────────────────────────────────────────────────────┘
```

### Stack Navigator (თითოეული ტაბის შიგნით)

**სერვისების Stack:**
```
Services (კატეგორიები)
    └── Category (კატეგორიის სერვისები)
            └── ServiceDetail (დეტალური გვერდი)
                    └── MechanicProfile (მექანიკის პროფილი)
```

**რუკის Stack:**
```
Map (მთავარი რუკა)
    └── ServiceDetail / LaundryDetail / DriveDetail
```

**საწვავის Stack:**
```
Fuel (იმპორტერების სია)
```

---

## გვერდების დეტალური აღწერა

### 1. სერვისების გვერდი (Services Tab)

#### 1.1 მთავარი გვერდი - კატეგორიების სია
**გზა:** `/` (Home tab)

**კომპონენტები:**
- Header ლოგოთი და საძიებო ღილაკით
- Hero section ბანერით (optional)
- კატეგორიების Grid (2x სვეტი)
- Pull-to-refresh

**მონაცემები:**
```typescript
interface ServiceCategory {
  id: number;
  name: string;
  description: string | null;
  icon: string | null;
}
```

**UI:**
```
┌─────────────────────────────────┐
│ [Logo]              [🔍]        │
├─────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐        │
│ │ 🔧      │ │ 🛞      │        │
│ │ ძრავა   │ │ საბურავი │        │
│ └─────────┘ └─────────┘        │
│ ┌─────────┐ ┌─────────┐        │
│ │ 🛑      │ │ ⚡      │        │
│ │ მუხრუჭი │ │ ელექტრო │        │
│ └─────────┘ └─────────┘        │
│            ...                  │
└─────────────────────────────────┘
```

#### 1.2 კატეგორიის სერვისები
**გზა:** `/category/[slug]`

**კომპონენტები:**
- Back button + კატეგორიის სახელი
- ფილტრების ზოლი (ქალაქი, რაიონი)
- სერვისების სია (FlatList)
- Pull-to-refresh
- Infinite scroll

**ფილტრები:**
```typescript
interface ServiceFilters {
  categoryId: number;
  cityId?: number;
  districtId?: number;
  search?: string;
}
```

**UI:**
```
┌─────────────────────────────────┐
│ [←] ძრავის შეკეთება             │
├─────────────────────────────────┤
│ [ქალაქი ▼] [რაიონი ▼] [🔍]     │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ [📷] სერვისის სახელი        │ │
│ │      ⭐ 4.8 (23 შეფასება)   │ │
│ │      📍 თბილისი, საბურთალო  │ │
│ │      💰 50-150 ₾            │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ [📷] სხვა სერვისი           │ │
│ │      ...                     │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

#### 1.3 სერვისის დეტალური გვერდი
**გზა:** `/service/[id]`

**სექციები:**
1. **გალერეა** - სურათების carousel
2. **ძირითადი ინფო** - სახელი, რეიტინგი, ფასი
3. **აღწერა** - სრული აღწერილობა
4. **მდებარეობა** - მისამართი + მინი რუკა
5. **მექანიკის ინფო** - კარტი პროფილზე გადასვლით
6. **დამატებითი** - მანქანის ბრენდები, გადახდა

**მოქმედებები:**
- 📞 დარეკვა (expo-linking)
- 🗺️ მისამართზე გადასვლა (Google Maps / Apple Maps)
- 📤 გაზიარება

**UI:**
```
┌─────────────────────────────────┐
│ [←]                      [📤]  │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │         [სურათების          │ │
│ │          Carousel]          │ │
│ │      • • ○ ○ ○              │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ სერვისის სახელი                 │
│ ⭐ 4.8 (23)  •  📍 თბილისი     │
│                                 │
│ 💰 50 - 150 ₾                   │
│ ⏱️ 2-3 საათი                    │
├─────────────────────────────────┤
│ აღწერა                          │
│ Lorem ipsum dolor sit amet...   │
├─────────────────────────────────┤
│ 📍 მდებარეობა                   │
│ თბილისი, საბურთალო, ...         │
│ ┌─────────────────────────────┐ │
│ │       [მინი რუკა]           │ │
│ └─────────────────────────────┘ │
│         [🗺️ მისამართი]         │
├─────────────────────────────────┤
│ 👤 მექანიკი                     │
│ ┌─────────────────────────────┐ │
│ │ [👤] გიორგი კ.    [ნახვა →] │ │
│ │     ⭐ 4.9 • 5 წლის გამოცდ. │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ [     📞 დარეკვა     ]         │
└─────────────────────────────────┘
```

#### 1.4 მექანიკის პროფილი
**გზა:** `/mechanic/[id]`

**სექციები:**
- პროფილის header (ავატარი, სახელი, რეიტინგი)
- სპეციალიზაცია და გამოცდილება
- სერვისების სია
- კონტაქტის ღილაკი

---

### 2. რუკის გვერდი (Map Tab)

#### 2.1 ძირითადი რუკა
**გზა:** `/map`

**კომპონენტები:**
- სეგმენტური ტაბები (სერვისები / სამრეცხაოები / დრაივები)
- საძიებო ზოლი (overlay)
- ფილტრები (კატეგორია, ქალაქი)
- რუკა მარკერებით
- Bottom Sheet სიით

**მარკერების ფერები (სერვისებისთვის):**
```typescript
const markerColors = {
  oil_engine: '#22c55e',      // მწვანე
  tire: '#1e40af',            // მუქი ლურჯი
  brake: '#dc2626',           // წითელი
  repair: '#3b82f6',          // ლურჯი
  gas: '#f97316',             // ნარინჯისფერი
  electrical: '#7c3aed',      // იისფერი
  body: '#ec4899',            // ვარდისფერი
  ac: '#06b6d4',              // ციანი
  mobile: '#14b8a6',          // ტილი
};
```

**UI:**
```
┌─────────────────────────────────┐
│ [🔍 მოძებნე...]                │
│ [კატეგორია ▼] [ქალაქი ▼] [✕]   │
├─────────────────────────────────┤
│ [სერვისები] [სამრეცხაოები] [დრაივები]
├─────────────────────────────────┤
│                                 │
│         📍                      │
│    📍        📍                 │
│        [რუკა]     📍            │
│    📍                           │
│              📍                 │
│                                 │
├─────────────────────────────────┤
│ ═══════════════════════════════ │ ← Drag handle
│ 24 სერვისი ნაპოვნია             │
│ ┌─────────────────────────────┐ │
│ │ სერვისი 1         [ნახვა]  │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ სერვისი 2         [ნახვა]  │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

**Marker Callout:**
```
┌─────────────────────────────────┐
│ [📷]  სერვისის სახელი          │
│       📍 მისამართი              │
│       [დეტალები →]             │
└─────────────────────────────────┘
```

**Bottom Sheet States:**
1. **Collapsed** - მხოლოდ drag handle და რაოდენობა
2. **Half-expanded** - სია ჩანს
3. **Expanded** - სრული სია

---

### 3. საწვავის გვერდი (Fuel Tab)

#### 3.1 იმპორტერების სია
**გზა:** `/fuel`

**კომპონენტები:**
- Header
- იმპორტერების სია (FlatList)
- Pull-to-refresh

**მონაცემები:**
```typescript
interface FuelImporter {
  id: number;
  name: string;
  logo: string | null;
  regular_ron_93_price: number | null;
  premium_ron_96_price: number | null;
  super_ron_98_price: number | null;
  created_at: string;
  updated_at: string;
}
```

**UI:**
```
┌─────────────────────────────────┐
│ ⛽ საწვავის ფასები              │
├─────────────────────────────────┤
│ ბოლო განახლება: 12 იანვარი     │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ [Logo] სოკარ                │ │
│ │ ─────────────────────────── │ │
│ │ RON 93    RON 96    RON 98  │ │
│ │ 2.89 ₾    3.15 ₾    3.45 ₾  │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ [Logo] გალფ                 │ │
│ │ ─────────────────────────── │ │
│ │ RON 93    RON 96    RON 98  │ │
│ │ 2.85 ₾    3.12 ₾    3.42 ₾  │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ [Logo] ვისოლი               │ │
│ │ ...                          │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

**ფასების ფორმატირება:**
- ყველაზე იაფი ფასი მწვანედ
- ყველაზე ძვირი ფასი წითლად
- დანარჩენი ნეიტრალური

---

## კომპონენტების დეტალური აღწერა

### UI კომპონენტები

#### Button
```typescript
interface ButtonProps {
  variant: 'primary' | 'secondary' | 'outline' | 'ghost';
  size: 'sm' | 'md' | 'lg';
  children: React.ReactNode;
  onPress: () => void;
  disabled?: boolean;
  loading?: boolean;
  icon?: string;
}
```

#### Card
```typescript
interface CardProps {
  children: React.ReactNode;
  onPress?: () => void;
  style?: ViewStyle;
}
```

#### Badge
```typescript
interface BadgeProps {
  variant: 'default' | 'success' | 'warning' | 'error';
  children: string;
}
```

### Service კომპონენტები

#### ServiceCard
```typescript
interface ServiceCardProps {
  service: MechanicService;
  onPress: () => void;
}
```
აჩვენებს: სურათი, სახელი, რეიტინგი, მდებარეობა, ფასი

#### ServiceGallery
```typescript
interface ServiceGalleryProps {
  photos: string[];
  videos?: string[];
}
```
Carousel სურათებით და ვიდეოებით

#### ServiceFilters
```typescript
interface ServiceFiltersProps {
  filters: ServiceFilters;
  onFiltersChange: (filters: ServiceFilters) => void;
}
```

### Map კომპონენტები

#### CustomMarker
```typescript
interface CustomMarkerProps {
  coordinate: { latitude: number; longitude: number };
  color: string;
  isSelected: boolean;
  onPress: () => void;
}
```

#### MarkerCallout
```typescript
interface MarkerCalloutProps {
  item: MechanicService | Laundry | Drive;
  type: 'service' | 'laundry' | 'drive';
  onDetailsPress: () => void;
}
```

#### MapBottomSheet
```typescript
interface MapBottomSheetProps {
  items: Array<MechanicService | Laundry | Drive>;
  type: 'service' | 'laundry' | 'drive';
  onItemPress: (item: any) => void;
  onItemSelect: (item: any) => void; // Centers map
}
```

---

## Hooks

### useServices
```typescript
const useServices = (filters: ServiceFilters) => {
  return useQuery({
    queryKey: ['services', filters],
    queryFn: () => fetchServices(filters),
  });
};
```

### useLaundries
```typescript
const useLaundries = (filters: LaundryFilters) => {
  return useQuery({
    queryKey: ['laundries', filters],
    queryFn: () => fetchLaundries(filters),
  });
};
```

### useDrives
```typescript
const useDrives = (filters: DriveFilters) => {
  return useQuery({
    queryKey: ['drives', filters],
    queryFn: () => fetchDrives(filters),
  });
};
```

### useFuelImporters
```typescript
const useFuelImporters = () => {
  return useQuery({
    queryKey: ['fuelImporters'],
    queryFn: fetchFuelImporters,
  });
};
```

### useLocation
```typescript
const useLocation = () => {
  // expo-location integration
  // Returns current user location
  // Requests permissions
};
```

---

## მონაცემთა მოდელები (რეპლიკაცია ვებიდან)

### MechanicService
```typescript
interface MechanicService {
  id: number;
  mechanic_id: string;
  category_id: number;
  name: string;
  description: string | null;
  price_from: number | null;
  price_to: number | null;
  estimated_hours: number | null;
  city: string | null;
  district: string | null;
  address: string | null;
  latitude: number | null;
  longitude: number | null;
  car_brands: string[] | null;
  on_site_service: boolean | null;
  accepts_card_payment: boolean | null;
  accepts_cash_payment: boolean | null;
  photos: string[] | null;
  videos: string[] | null;
  rating: number | null;
  review_count: number | null;
  is_active: boolean | null;
  // Relations
  mechanic_profiles?: MechanicProfile;
  service_categories?: ServiceCategory;
}
```

### Laundry
```typescript
interface Laundry {
  id: number;
  name: string;
  description: string | null;
  city: string | null;
  district: string | null;
  address: string | null;
  latitude: number | null;
  longitude: number | null;
  phone: string | null;
  working_hours: string | null;
  photos: string[] | null;
  rating: number | null;
  review_count: number | null;
}
```

### Drive
```typescript
interface Drive {
  id: number;
  name: string;
  description: string | null;
  city: string | null;
  district: string | null;
  address: string | null;
  latitude: number | null;
  longitude: number | null;
  phone: string | null;
  photos: string[] | null;
}
```

---

## დიზაინის სისტემა

### ფერები (საიტის მსგავსი)
```typescript
const colors = {
  primary: {
    50: '#eff6ff',
    100: '#dbeafe',
    500: '#3b82f6',   // ძირითადი
    600: '#2563eb',
    700: '#1d4ed8',
  },
  secondary: {
    500: '#6366f1',
  },
  success: {
    500: '#22c55e',
  },
  warning: {
    500: '#f59e0b',
  },
  error: {
    500: '#ef4444',
  },
  gray: {
    50: '#f9fafb',
    100: '#f3f4f6',
    200: '#e5e7eb',
    300: '#d1d5db',
    400: '#9ca3af',
    500: '#6b7280',
    600: '#4b5563',
    700: '#374151',
    800: '#1f2937',
    900: '#111827',
  },
  background: '#ffffff',
  surface: '#f9fafb',
  text: {
    primary: '#111827',
    secondary: '#6b7280',
  },
};
```

### Typography
```typescript
const typography = {
  h1: { fontSize: 28, fontWeight: '700', lineHeight: 34 },
  h2: { fontSize: 24, fontWeight: '600', lineHeight: 30 },
  h3: { fontSize: 20, fontWeight: '600', lineHeight: 26 },
  h4: { fontSize: 18, fontWeight: '600', lineHeight: 24 },
  body: { fontSize: 16, fontWeight: '400', lineHeight: 24 },
  bodySmall: { fontSize: 14, fontWeight: '400', lineHeight: 20 },
  caption: { fontSize: 12, fontWeight: '400', lineHeight: 16 },
};
```

### Spacing
```typescript
const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};
```

### Border Radius
```typescript
const borderRadius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
};
```

---

## Native ინტეგრაციები

### ტელეფონზე დარეკვა
```typescript
import * as Linking from 'expo-linking';

const callPhone = (phoneNumber: string) => {
  Linking.openURL(`tel:${phoneNumber}`);
};
```

### რუკაზე გადასვლა
```typescript
import * as Linking from 'expo-linking';
import { Platform } from 'react-native';

const openMaps = (latitude: number, longitude: number, label: string) => {
  const scheme = Platform.select({
    ios: 'maps:',
    android: 'geo:',
  });
  const url = Platform.select({
    ios: `maps:?q=${label}&ll=${latitude},${longitude}`,
    android: `geo:${latitude},${longitude}?q=${latitude},${longitude}(${label})`,
  });
  Linking.openURL(url);
};
```

### გაზიარება
```typescript
import { Share } from 'react-native';

const shareService = async (service: MechanicService) => {
  await Share.share({
    title: service.name,
    message: `${service.name} - ${service.address}`,
    url: `https://fixmyride.ge/service/${service.id}`,
  });
};
```

### მდებარეობა
```typescript
import * as Location from 'expo-location';

const getCurrentLocation = async () => {
  const { status } = await Location.requestForegroundPermissionsAsync();
  if (status !== 'granted') return null;

  const location = await Location.getCurrentPositionAsync({});
  return location.coords;
};
```

---

## რუკის კონფიგურაცია

### OpenStreetMap Tiles
```typescript
// MapView component configuration
<MapView
  provider={PROVIDER_DEFAULT}
  customMapStyle={[]} // Optional custom styling
  urlTemplate="https://tile.openstreetmap.org/{z}/{x}/{y}.png"
>
```

### საწყისი რეგიონი (საქართველო)
```typescript
const initialRegion = {
  latitude: 41.7151,
  longitude: 44.8271,
  latitudeDelta: 2.5,
  longitudeDelta: 2.5,
};

// თბილისის ცენტრი
const tbilisiCenter = {
  latitude: 41.7151,
  longitude: 44.8271,
  latitudeDelta: 0.1,
  longitudeDelta: 0.1,
};
```

---

## ბიბლიოთეკების სია

### Core
```json
{
  "expo": "~50.0.0",
  "react": "18.2.0",
  "react-native": "0.73.0",
  "typescript": "^5.3.0"
}
```

### Navigation
```json
{
  "expo-router": "~3.4.0",
  "@react-navigation/native": "^6.1.0",
  "@react-navigation/bottom-tabs": "^6.5.0"
}
```

### State Management
```json
{
  "@tanstack/react-query": "^5.0.0",
  "@supabase/supabase-js": "^2.39.0"
}
```

### UI
```json
{
  "react-native-paper": "^5.12.0",
  "nativewind": "^4.0.0",
  "react-native-reanimated": "~3.6.0",
  "react-native-gesture-handler": "~2.14.0",
  "@gorhom/bottom-sheet": "^4.6.0"
}
```

### Maps & Location
```json
{
  "react-native-maps": "1.8.0",
  "expo-location": "~16.5.0"
}
```

### Utilities
```json
{
  "expo-linking": "~6.2.0",
  "expo-image": "~1.10.0",
  "date-fns": "^3.0.0",
  "react-hook-form": "^7.49.0",
  "zod": "^3.22.0"
}
```

---

## იმპლემენტაციის გეგმა

### ფაზა 1: საფუძვლის შექმნა
- [ ] Expo პროექტის ინიციალიზაცია
- [ ] ფოლდერის სტრუქტურის შექმნა
- [ ] TypeScript კონფიგურაცია
- [ ] Supabase კლიენტის დაყენება
- [ ] React Query კონფიგურაცია
- [ ] Navigation სტრუქტურა
- [ ] დიზაინის სისტემა (ფერები, ტიპოგრაფია)

### ფაზა 2: UI კომპონენტები
- [ ] Button, Card, Badge, Input
- [ ] Skeleton loading states
- [ ] Empty state, Error state
- [ ] Header, SearchBar

### ფაზა 3: სერვისების მოდული
- [ ] Categories grid
- [ ] Services list with filters
- [ ] Service detail page
- [ ] Mechanic profile page
- [ ] Phone call integration
- [ ] Share functionality

### ფაზა 4: რუკის მოდული
- [ ] Map view setup
- [ ] Custom markers
- [ ] Filters overlay
- [ ] Bottom sheet with list
- [ ] Tab switching (Services/Laundries/Drives)
- [ ] Location permission & current location
- [ ] Navigate to maps app

### ფაზა 5: საწვავის მოდული
- [ ] Fuel importers list
- [ ] Price cards with comparison
- [ ] Pull-to-refresh

### ფაზა 6: ტესტირება და გამოშვება
- [ ] Testing on iOS simulator
- [ ] Testing on Android emulator
- [ ] Performance optimization
- [ ] Build for production
- [ ] App Store / Play Store submission

---

## შენიშვნები

### რაც არ შედის ამ ვერსიაში:
- ❌ მომხმარებლის ავტორიზაცია
- ❌ ჯავშნის სისტემა
- ❌ შეფასებების დაწერა
- ❌ შენახული სერვისები
- ❌ ჩატი
- ❌ საწვავზე ხმის მიცემა
- ❌ კალკულატორი
- ❌ Push notifications
- ❌ Dark mode (შეიძლება დაემატოს მოგვიანებით)

### მომავალი განვითარება:
- 🔄 ავტორიზაციის დამატება
- 🔄 ჯავშნის ფუნქციონალი
- 🔄 Push notifications
- 🔄 Offline support
- 🔄 Dark mode

---

## დოკუმენტაციის ბმულები

- [Expo Documentation](https://docs.expo.dev/)
- [React Native Maps](https://github.com/react-native-maps/react-native-maps)
- [React Query](https://tanstack.com/query/latest)
- [Supabase JS](https://supabase.com/docs/reference/javascript)
- [React Navigation](https://reactnavigation.org/docs/getting-started)
- [Gorhom Bottom Sheet](https://gorhom.github.io/react-native-bottom-sheet/)
